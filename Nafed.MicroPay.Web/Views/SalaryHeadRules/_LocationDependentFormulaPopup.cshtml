@using Nafed.MicroPay.Model;
@model SalaryHead
@Html.HiddenFor(model=>model.EmployeeTypeID)


    <div class="row">
        <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-xs-12 pt-2" id="div-branch-sal-head-rule-form" style="max-height:400px;">
               
               @Html.Partial("_LocationFormulaEditor", Model)
 
            <hr />
        </div>
    </div>

    <div class="row pr-2">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12" id="div-branch-sal-head-rule-list" style="max-height:300px; overflow-y:scroll;">
            @Html.Action("_GetBranchSalaryHeadRules",new { @fieldName= Model.FieldName, @employeeTypeID=Model.EmployeeTypeID })

        </div>
    </div>
@*}*@

<script type="text/javascript">
    function OnLocFormulaSuccess(data) {

        if(data.fieldName!=undefined)
        {
            //$("#ActionType").val("Update");
            //$("#div-branch-sal-head-rule-form").html(data.formulaForm);
            //$("#div-branch-sal-head-rule-list").load("/SalaryHeadRules/_GetBranchSalaryHeadRules?fieldName=" + data.fieldName);
            var defaultHref = $("#lnkLocFormulaEditor").attr('href');
           
            $('#div-bd').load(defaultHref);
            commonMessage(data.type, data.msg);
          // $("#LocationModel").modal('hide');
        }
        else {
            $("#div-branch-sal-head-rule-form").html(data);
        }
    }


</script>