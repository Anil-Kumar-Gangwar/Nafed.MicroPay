@model Nafed.MicroPay.Model.UserAccessRight

@{
    ViewBag.Title = "Bank";
    Layout = "~/Views/Shared/_Layout.cshtml";
    System.Globalization.CultureInfo Indian = new System.Globalization.CultureInfo("hi-IN");

}

<div class="">
    <div class="card_new_header text-right">
        @if (Model.Create)
        {
            @Html.ActionLink("Create New", "Create", "Bank", null, new { @class = "btn btn-info btn-sm", title = "Click here, to create new record", @data_toggle = "tooltip" })
        }
    </div>
    <div class="card_new_body">
        <div id="BankGridDiv">
            @Html.Action("BankGridView")
        </div>
    </div>
</div>
