@model MicroPay.Web.Models.LeavePurposeViewModel

<table id="tblUser" class="table table-centered table-striped dt-responsive w-100">
    <thead>
        <tr>
            <th>
                #
            </th>
            <th>
                Leave Category
            </th>
            <th>
                Leave Purpose
            </th>
            @if (Model.userRights.Edit)
            {
                <th class="text-center" style="width:60px">
                    Edit
                </th>
            }
            @if (Model.userRights.Delete)
            {
                <th class="text-center" style="width:60px">
                    Delete
                </th>
            }
        </tr>
    </thead>
    <tbody>
        @{
            int index = 1;
            if (Model != null && Model.leavePurposeList.Count > 0)
            {
                foreach (var item in Model.leavePurposeList)
                {
                    <tr>
                        <td>@index</td>
                        <td> @item.LeavePurposeName</td>
                        <td> @item.CategoryName</td>
                        @if (Model.userRights.Edit)
                        {
                            <td class="text-center">
                                @*<a href="@Url.Action("Edit", "LeavePurpose", new { leavePurposeID = @item.LeavePurposeID })" title="Click here, to edit this record" data-toggle="tooltip">
                                    <span class="far fa-edit text-info" aria-hidden="true"></span>
                                </a>*@
                                @Html.EncodedImageActionLink("", "Edit", "LeavePurpose", new { leavePurposeID = @item.LeavePurposeID }, new { @class = "toggle-tooltip", title = "Click here, to edit this record" }, "~/Images/edit.png")
                            </td>
                        }
                        @if (Model.userRights.Delete)
                        {
                            <td class="text-center">
                                @*<a class="toggle-tooltip" data-toggle="confirm" href="@Url.Action("Delete", "LeavePurpose", new { leavePurposeID = @item.LeavePurposeID })" title="Click here, to delete this record">
                                    <span class="far fa-trash-alt text-danger" aria-hidden="true"></span>
                                </a>*@
                                @Html.EncodedImageActionLink("", "Delete", "LeavePurpose", new { leavePurposeID = @item.LeavePurposeID }, new { @class = "toggle-tooltip", title = "Click here, to delete this record" }, "~/Images/delete.png", true)
                            </td>
                        }
                    </tr>
                    index++;
                }
            }

        }

    </tbody>
</table>


